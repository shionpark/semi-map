#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 타입 체크 중..."
pnpm exec tsc --noEmit --skipLibCheck || (echo "❌ 타입 에러! 수정 후 다시 커밋해 주세요." && exit 1)

echo "🔍 테스트 실행 중..."
pnpm test || (echo "❌ 테스트 실패!" && exit 1)

echo "✅ 모든 검사 통과! 푸시를 진행합니다."